version: "2"
sql: 
    # Uncomment this if using a migration tool
    # - schema : "db/migrations/"
    - schema : "db/schema.sql"
      queries: "db/query.sql"
      engine: "sqlite"
      gen:
        go: 
          package: "data"
          out: "db"
          emit_json_tags: true

