# Authentication module

This module was generated for `@@PROJECT_NAME@@` (`@@ORG@@`).

- Database backend: `@@DATABASE@@`
- Session lifetime: `@@AUTH_SESSION_TTL@@` hours
- Parent values were inherited explicitly from the root manifest
- Module-specific values were collected locally

The module owns authentication-specific UI, handlers, and migrations. Keep
project-wide settings in the root manifest and add new module settings here.
