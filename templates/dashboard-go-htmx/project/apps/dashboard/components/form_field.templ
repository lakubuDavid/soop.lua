package components

templ FormField(label string, name string, inputType string) {
	<label class="block text-sm font-medium text-[var(--color-text)]">{ label }<input class="input-base mt-1" type={ inputType } name={ name }/></label>
}
